const {chromium}=require('C:/Users/Sidharth Mantri/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const fs=require('fs');
(async()=>{const b=await chromium.launch({headless:true,channel:'msedge'});const p=await b.newPage({viewport:{width:1366,height:900}});p.setDefaultTimeout(12000);await p.addInitScript(()=>{if(!localStorage.getItem('@interny_user_v2'))localStorage.setItem('@interny_user_v2',JSON.stringify({user:{name:'Browser QA',grade:'11th Grade',interests:['Science'],state:'VA',onboardingDone:true,onboardingVersion:2,featureTourSeen:true,notificationsOn:false}}));});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://localhost:5173/internships');let f=p.frames().find(f=>f.url().includes('/interny-app/'));await f.getByText('811 found',{exact:true}).waitFor();await f.getByText('Search',{exact:true}).click();const input=f.getByRole('textbox',{name:'Search internships by role, company, field, or tag'});await input.fill('zzzz-unmatched-qa');await f.getByText(/No results for/).waitFor();await f.getByRole('button',{name:'Clear search and filters'}).click();await f.getByRole('button',{name:'Filters',exact:true}).click();await f.getByText('Free',{exact:true}).click();await f.getByText('Clear all',{exact:true}).click();console.log('FILTER',(await f.locator('body').innerText()).slice(-1200));await f.getByText(/Show .*results/).click();await input.fill('NASA');await f.getByRole('button',{name:'Deadline',exact:true}).click();await f.getByText('NASA OSTEM High School Internship',{exact:true}).first().click();await f.getByRole('button',{name:'Save internship',exact:true}).click();await f.getByRole('button',{name:'Add to top picks',exact:true}).click();console.log('DETAIL',(await f.locator('body').ariaSnapshot()).slice(-3500));await p.reload();f=p.frames().find(f=>f.url().includes('/interny-app/'));await f.getByRole('button',{name:'Remove from saved',exact:true}).waitFor();await f.getByRole('button',{name:'Remove from top picks',exact:true}).waitFor();await p.goBack();await f.getByRole('textbox',{name:'Search internships by role, company, field, or tag'}).waitFor();console.log('back-and-refresh-pass');for(const width of [1440,1366,768,390]){await p.setViewportSize({width,height:900});await p.screenshot({path:`interny-qa-${width}.png`});const overflow=await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth);console.log('width',width,'pageOverflow',overflow);if(overflow)throw Error('horizontal overflow');}console.log('ERRORS',errors);await p.context().storageState({path:'interny-qa-storage.json'});await b.close();})().catch(e=>{console.error(e);process.exit(1)});
